# Flask with database

There is only one route */user*, it is CRUD for user.
There is only one table in database - users.

By default, it creates database in sqlite, if you want to use another DB engine 
please change config.py.